import 'package:flutter/material.dart';
import 'package:flutter_hackathon/shared/libs/custom_palette.dart';

// TODO: add project palette ColorScheme (...)
final themePalette = ColorScheme.fromSeed(seedColor: Colors.black45);

// TODO: add custom colors or remove this
const customPalette = CustomPalette(exampleColor: Colors.black45);
