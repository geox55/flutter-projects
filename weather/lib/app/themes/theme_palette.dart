import 'package:flutter/material.dart';
import 'package:hackathon_demo/shared/libs/custom_palette.dart';

// TODO: add project palette ColorScheme (...)
final themePalette = ColorScheme.fromSeed(seedColor: Colors.purpleAccent);

// TODO: add custom colors or remove this
const customPalette = CustomPalette(exampleColor: Colors.red);
